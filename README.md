
 database is in uploads/db
 config database
    'hostname' => 'localhost',
    'port' => '',
    'username' => 'root',
    'password' => '',
    'database' => 'phone',
  if want to modified port or username password go to application/config/database.php
  if want to modified base_url go to application/config/config.php (incase local server have to had port)
